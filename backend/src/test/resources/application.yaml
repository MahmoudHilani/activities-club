spring:
  datasource:
    url: jdbc:h2:mem:activitiesclub;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

app:
  jwt:
    secret: test-only-jwt-secret-that-is-at-least-32-bytes
    expiration-ms: 900000
